package:
    name: estoktp
    version: "1.10.3"

source:
    path: .

requirements:
    build:
        - cmake
        - {{ compiler('c') }}
        - {{ compiler('fortran') }}
    run:
        - cmake
        - {{ compiler('c') }}
        - {{ compiler('fortran') }}
        - mess
