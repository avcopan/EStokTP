package:
    name: estoktp
    version: "1.10.2"

source:
    path: .

requirements:
    build:
        - cmake
        - {{ compiler('c') }}
        - {{ compiler('fortran') }}
    run:
        - mess
